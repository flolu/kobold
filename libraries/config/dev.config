# This config file is only used for local development only.
# Syntax: https://github.com/motdotla/dotenv#rules

ENVIRONMENT=dev
PRETTY_LOGGING=true

BASE_DOMAIN=localhost
CLIENT_URL=http://localhost:4000
